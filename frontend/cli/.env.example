# Copy to `.env` and fill in. Mirror the dashboard's VITE_API_URL.
# Base URL of the Django Ninja backend (scheme://host:port).
API_URL=http://localhost:8000/api
# Optional: how long to wait for a response (seconds)
API_TIMEOUT=30
